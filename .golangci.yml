version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - gosimple
    - gosec
    - govet
    - ineffassign
    - staticcheck
    - typecheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
