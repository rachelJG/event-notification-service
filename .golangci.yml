version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - gosec
    - govet
    - ineffassign
    - staticcheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
